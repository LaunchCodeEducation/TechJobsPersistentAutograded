@model TechJobsPersistentAutograded.ViewModels.AddEmployerViewModel

<h1>Add New Employer</h1>

<form asp-controller="Employer" asp-action="ProcessAddEmployerForm" method="post">
    <div class="form-group">
        <label asp-for="Name">Employer Name</label>
        <input asp-for="Name" />
        <span asp-validation-for="Name"></span>
    </div>
    <div class="form-group">
        <label asp-for="Location">Employer Location</label>
        <input asp-for="Location" />
        <span asp-validation-for="Location"></span>
    </div>
    <input type="submit" value="Submit" />
</form>


@*
        Test Set 2 -- Views

        filled by ProcessAddEmployerForm's Add method in Employer Controller
            test if not null?
            test with dummy view comparison??

        Starter Code doesn't have @model

        ------

        <h1>Add New Employer</h1>

    <form asp-controller="Employer" asp-action="ProcessAddEmployerForm" method="post">
        <div class="form-group">
            <label>Employer Name</label>
            <input />
        </div>
        <div class="form-group">
            <label>Employer Location</label>
            <input />
        </div>
        <input type="submit" value="Submit" />
    </form>

*@